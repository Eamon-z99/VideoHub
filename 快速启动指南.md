# 🚀 VideoHub 快速启动指南

## 📁 新项目结构

```
VideoHub/
├── frontend/              # 前端项目
│   ├── main-app/        # 主应用（qiankun 容器）
│   └── VideoHub_web/    # 子应用（视频中心）
│
└── backend/              # 后端项目
    └── VideoBackend/    # Spring Boot 服务
```

## ⚡ 快速启动（3步）

### 步骤 1：启动子应用

打开**第一个终端**窗口：

```bash
cd frontend/VideoHub_web
npm install    # 首次运行需要安装依赖
npm run dev
```

✅ 子应用启动成功：`http://localhost:5173`

---

### 步骤 2：启动主应用

打开**第二个终端**窗口：

```bash
cd frontend/main-app
npm install    # 首次运行需要安装依赖
npm run dev
```

✅ 主应用启动成功：`http://localhost:8080`

---

### 步骤 3：访问应用

在浏览器中打开：

🌐 **http://localhost:8080/videohub**

---

## 🔧 后端启动（可选）

如果需要启动后端服务：

```bash
cd backend/VideoBackend
mvn spring-boot:run
```

或者使用 Maven Wrapper：

```bash
# Windows
.\mvnw.cmd spring-boot:run

# Linux/Mac
./mvnw spring-boot:run
```

⚠️ **注意**：如果后端端口与前端主应用冲突（都是 8080），需要修改后端配置文件中的端口。

---

## 📝 常见问题

### Q: 端口被占用怎么办？

**A:** 修改对应项目的配置文件中的端口：
- 子应用端口：`frontend/VideoHub_web/vite.config.js` 中的 `port: 5173`
- 主应用端口：`frontend/main-app/vite.config.js` 中的 `port: 8080`
- 后端端口：`backend/VideoBackend/src/main/resources/application.properties`

### Q: 子应用无法加载？

**A:** 确保：
1. 子应用已启动（`http://localhost:5173` 可访问）
2. 主应用中的 `entry` 地址正确（`frontend/main-app/src/main.js`）
3. 浏览器控制台没有 CORS 错误

### Q: 如何独立运行子应用？

**A:** 子应用可以独立运行，直接访问 `http://localhost:5173` 即可。

---

## 🎯 下一步

- 查看 [微前端重构说明.md](./微前端重构说明.md) 了解详细架构
- 查看 [启动说明.md](./启动说明.md) 了解详细启动步骤
- 查看 [重构完成总结.md](./重构完成总结.md) 了解项目重构详情

