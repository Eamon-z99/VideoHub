# VideoHub 前端项目

微前端项目，包含主应用和子应用。

## 📁 项目结构

```
frontend/
├── main-app/          # 主应用（qiankun 容器应用）
├── VideoHub_web/      # 子应用（视频中心）
└── package.json       # 统一管理脚本
```

## 🚀 快速开始

### 首次使用 - 安装所有依赖

```bash
cd frontend
npm install
```

**✨ 自动安装所有子应用的依赖！**

使用 npm workspaces，会自动检测并安装：
- frontend 目录的依赖（concurrently）
- main-app 的依赖
- VideoHub_web 的依赖
- **以及未来添加的任何新子应用**

### 启动开发环境

**一键启动所有应用（自动检测）：**

```bash
cd frontend
npm run dev
```

**✨ 自动检测所有包含 `dev` 脚本的子应用并启动！**

当前会启动：
- 子应用：`http://localhost:5173`
- 主应用：`http://localhost:8080`

**添加新子应用时，无需修改任何配置，脚本会自动检测并启动！**

然后访问：`http://localhost:8080/videohub`

### 单独启动

如果需要单独启动某个应用：

```bash
# 只启动子应用
npm run dev:sub

# 只启动主应用
npm run dev:main
```

## 📦 构建

### 构建所有应用

```bash
npm run build
```

**✨ 使用 npm workspaces，自动构建所有子应用！**

### 单独构建

```bash
# 构建子应用
npm run build:sub

# 构建主应用
npm run build:main
```

## 🔍 预览构建结果

```bash
npm run preview
```

**✨ 自动检测所有包含 `preview` 脚本的子应用并预览！**

## 🧹 清理

清理所有 node_modules 和 dist 目录：

```bash
npm run clean
```

**✨ 自动检测并清理所有子应用的依赖和构建产物！**

## 📝 可用脚本

| 脚本 | 说明 |
|------|------|
| `npm install` | **自动安装所有子应用的依赖**（使用 workspaces） |
| `npm run dev` | **自动检测并启动所有子应用** |
| `npm run dev:main` | 只启动主应用 |
| `npm run dev:sub` | 只启动子应用 |
| `npm run build` | **自动构建所有子应用**（使用 workspaces） |
| `npm run build:main` | 构建主应用 |
| `npm run build:sub` | 构建子应用 |
| `npm run preview` | **自动检测并预览所有子应用** |
| `npm run clean` | **自动清理所有子应用的依赖和构建产物** |

## ✨ 智能特性

### 自动检测机制

- ✅ **添加新子应用时，无需修改任何配置**
- ✅ 脚本会自动检测所有包含 `package.json` 的子目录
- ✅ `npm run dev` 会自动启动所有有 `dev` 脚本的应用
- ✅ `npm run preview` 会自动预览所有有 `preview` 脚本的应用
- ✅ `npm run clean` 会自动清理所有子应用的依赖

### 如何添加新子应用

1. 在 `frontend` 目录下创建新目录（如 `new-app`）
2. 在新目录中创建 `package.json` 并配置 `dev` 脚本
3. 运行 `npm install` 安装依赖
4. 运行 `npm run dev`，新应用会自动被检测并启动！

**无需修改任何配置文件！**

## 💡 提示

- 使用 `npm run dev` 时，终端会显示不同颜色的输出，方便区分各个应用
- 每个应用使用不同的颜色标识（cyan, magenta, green, yellow 等）
- 使用 npm workspaces 可以统一管理依赖，减少重复安装

